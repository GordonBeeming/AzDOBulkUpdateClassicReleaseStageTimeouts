name: Build and Release

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
        
    - name: 'update version'
      id: update-version
      shell: bash
      run: |
        buildDateNumber=$(date '+%Y.%m.%d')
        buildId=$GITHUB_RUN_NUMBER
        buildNumber="$buildDateNumber.$buildId"
        echo "$buildNumber"
        projContent=$(sed "s+<Version>[0-9][0-9]*.*[0-9]*.*[0-9]*.*</Version>+<Version>$buildNumber</Version>+g" AzDOBulkUpdateClassicReleaseStageTimeouts.csproj)
        echo "$projContent" > AzDOBulkUpdateClassicReleaseStageTimeouts.csproj
        echo "::workflow-command BUILD_NUMBER=$buildNumber"
      working-directory: AzDOBulkUpdateClassicReleaseStageTimeouts
    
    - name: Install dependencies
      run: dotnet restore /nologo
      working-directory: AzDOBulkUpdateClassicReleaseStageTimeouts
    - name: Build
      run: dotnet build --configuration Release --no-restore /nologo
      working-directory: AzDOBulkUpdateClassicReleaseStageTimeouts
    - name: Test
      run: dotnet test --no-restore --no-build --verbosity normal /nologo
      working-directory: AzDOBulkUpdateClassicReleaseStageTimeouts
    - name: Publish
      run: dotnet publish --configuration Release --no-restore --no-build --output "../package" --verbosity normal /nologo /property:GenerateFullPaths=true
      working-directory: AzDOBulkUpdateClassicReleaseStageTimeouts

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
      with:
        tag_name: ${{ steps.update-version.outputs.BUILD_NUMBER }}
        release_name: Release ${{ steps.update-version.outputs.BUILD_NUMBER }}
        body: |
          
        draft: false
        prerelease: false
